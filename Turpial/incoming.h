/**
   procedimientos para manejo de paquetes entrantes
*/

extern char*  id_node;
extern packet_t Buffer_packet;

int routing_incoming_PACKET_HELLO(char*  id_node, packet_t packet_received){

  return 0;
}
